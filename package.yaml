name: hpre
version: 1.6.2
license: MIT
author: Galen Huntington
synopsis: Simple but handy Haskell preprocessor.
category: Language

executable:
   main: hpre.hs

data-files:
- "tests/*"

tests:
   test:
      main: test.hs
      dependencies:
      - process
      - directory

dependencies:
   base: 4.*
   mtl: 2.*

